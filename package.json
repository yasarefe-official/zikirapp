{
  "name": "zikir-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Zikir App Monorepo with PNPM",
  "scripts": {
    "dev": "pnpm --filter client start",
    "start": "pnpm --filter server start",
    "build": "pnpm --filter client build"
  },
  "engines": {
    "node": "20.x",
    "pnpm": ">=8"
  }
}
